import * as configPresentationData from './constants';
import * as presentationActions from './actions';
import presentationReducer, { ConfigState, ConfigAction } from './reducer';

export {
    configPresentationData,
    presentationActions,
    presentationReducer,
    ConfigState,
    ConfigAction
};
